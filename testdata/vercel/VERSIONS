# Resolved versions of the packages this capture declared.
# Written by testdata/capture/capture.sh.
#
# Nothing is pinned on purpose: re-capturing is meant to pick up new
# releases, because a library changing what it emits is the drift this
# harness exists to notice. This file records what it was, so that a
# change in KEYS can be told apart from a change in nothing.
# captured: 2026-09-11T10:38:51Z

@ai-sdk/openai==2.0.125
@opentelemetry/api==1.9.1
@opentelemetry/exporter-trace-otlp-http==0.222.0
@opentelemetry/resources==2.11.0
@opentelemetry/sdk-trace-node==2.11.0
@opentelemetry/semantic-conventions==1.43.0
ai==5.0.253
zod==4.5.4
