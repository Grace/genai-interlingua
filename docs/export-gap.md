# What each export format can carry

<!-- Generated by `go test ./internal/emit -update`. Do not edit. -->

There are three ways a mapping leaves this repository, and they are not
equivalent. The processor carries everything, because it is Go and Go can read
a span. [OTTL](https://github.com/open-telemetry/opentelemetry-collector-contrib/blob/main/pkg/ottl/README.md)
carries the mappings that are transformations of a *value*. An [OpenTelemetry
Telemetry Schema File](https://opentelemetry.io/docs/specs/otel/schemas/file_format_v1.1.0/)
carries only the ones that are changes of a *name*.

This table is measured from the rule tables, not described. It is the evidence
for [`docs/oteps/`](oteps/): a proposal to add transformation types to a format
is worth the count of real mappings that currently need them, and this is where
that count comes from.

## Coverage

| dialect | target | mappings | representable | processor | OTTL | schema file |
| --- | --- | ---: | ---: | ---: | ---: | ---: |
| `litellm` | `v1.41.0` | 24 | 24 | 24 | 20 | 18 |
| `litellm` | `genai-main` | 24 | 24 | 24 | 20 | 18 |

**mappings** is every field the dialect can produce. **representable** is how
many of those the target schema has an attribute for at all — a field the
target cannot name is not a failure of an export format, so the last three
columns are measured against it rather than against the first.

The schema file column needs one more split, because most of it is not the
format succeeding:

| dialect | target | renames written | needed no rename | could not be expressed |
| --- | --- | ---: | ---: | ---: |
| `litellm` | `v1.41.0` | 0 | 18 | 6 |
| `litellm` | `genai-main` | 1 | 17 | 6 |

"Needed no rename" is the emitter already writing the target's own attribute
name. It is a property of the emitter, not of the format; counting it as
coverage would credit the schema file for work nobody did.

## Why the gaps are gaps

Every mapping a schema file could not carry, grouped by the specific
capability that was missing. The point of naming them this precisely is that
"schema files are not expressive enough" is not a proposal, and "schema files
cannot rewrite a value" is.

| reason | what it means | count |
| --- | --- | ---: |
| `value_transform` | the value changes, not the name: provider aliases, operation aliases, case folding, unit conversion | 4 |
| `not_a_name` | a reading of the span rather than a transformation of an attribute | 8 |

`value_transform` is what a format change could fix: 4 mappings that fail only because
the format has no transformation for them. That is the room
[OTEP 0152](https://github.com/open-telemetry/opentelemetry-specification/blob/main/oteps/0152-telemetry-schemas.md)
left when it held the transformation set to "the bare minimum ... with more
types of transformations potentially proposed in the future".

`not_a_name` is not one of them. Reassembling `gen_ai.prompt.{i}.tool_calls.{j}.*`
into one document is a reading of a span, and no declarative schema format
should try to express it. That one stays in code, and the honest thing to do
about it is what this repository does: say so on the span.

## The sample

1 of 6 dialects declare their mappings as data so far, so that is what can be
measured. The rest are migrating incrementally, and until one is migrated it
contributes nothing to this table rather than contributing a zero — an
unmigrated dialect is unmeasured, not unexportable-in-principle, and the
difference matters to anything that cites these counts.
