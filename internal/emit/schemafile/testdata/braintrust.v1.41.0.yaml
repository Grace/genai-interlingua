# Generated by genai-interlingua: interlingua -emit schema-file -dialect braintrust -target v1.41.0
#
# An OpenTelemetry Telemetry Schema File (format 1.1.0) expressing as much of
# the braintrust mapping as that format can carry.
#
#     1 written here as rename_attributes
#     2 need no rename: braintrust already writes the target's own attribute name
#    10 cannot be expressed in this format at all
#
# Read the gap list below before using this. The transformations a schema
# file supports are all changes of NAME: rename_attributes, rename_events,
# rename_metrics, and a metrics-only split. Everything this mapping does to a
# VALUE -- provider aliases, unit conversion, case folding, dropping a value
# the target does not admit -- has no expression here at all.
#
# Applying this file alone therefore produces spans with the right attribute
# names carrying values the target schema does not define. That is not a bug
# in the generator; it is the shortfall, written out.

# WHAT THIS FILE CANNOT SAY (10 mappings)
#
#   closed_value_set
#     gen_ai.provider.name                     gen_ai.provider.name admits 15 values; a schema file cannot drop one it does not
#
#   not_a_name
#     gen_ai.evaluation.name                   produced by reading the span, not by renaming an attribute
#     gen_ai.evaluation.score.value            produced by reading the span, not by renaming an attribute
#     gen_ai.input.messages                    produced by reading the span, not by renaming an attribute
#     gen_ai.operation.name                    produced by reading the span, not by renaming an attribute
#     gen_ai.output.messages                   produced by reading the span, not by renaming an attribute
#     gen_ai.usage.cache_read.input_tokens     produced by reading the span, not by renaming an attribute
#     gen_ai.usage.input_tokens                produced by reading the span, not by renaming an attribute
#     gen_ai.usage.output_tokens               produced by reading the span, not by renaming an attribute
#     gen_ai.usage.reasoning.output_tokens     produced by reading the span, not by renaming an attribute
#
file_format: 1.1.0

# Version 1.0.0 of this schema is the normalized form. Version 0.0.0 is the
# braintrust vocabulary the spans arrive in. Calling a vendor's dialect a prior
# version of the conventions is a liberty -- it is a sibling, not an
# ancestor -- and it is taken here because the format offers no other way to
# relate two vocabularies. That liberty is itself part of the argument.
schema_url: https://github.com/Grace/genai-interlingua/schemas/braintrust/v1.41.0/1.0.0

versions:
  0.0.0:

  1.0.0:
    spans:
      changes:
        - rename_attributes:
            attribute_map:
              gen_ai.agent.tools: gen_ai.tool.definitions
