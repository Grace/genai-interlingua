# Generated by genai-interlingua: interlingua -emit schema-file -dialect openllmetry -target v1.41.0
#
# An OpenTelemetry Telemetry Schema File (format 1.1.0) expressing as much of
# the openllmetry mapping as that format can carry.
#
#     9 written here as rename_attributes
#     6 need no rename: openllmetry already writes the target's own attribute name
#    12 cannot be expressed in this format at all
#
# Read the gap list below before using this. The transformations a schema
# file supports are all changes of NAME: rename_attributes, rename_events,
# rename_metrics, and a metrics-only split. Everything this mapping does to a
# VALUE -- provider aliases, unit conversion, case folding, dropping a value
# the target does not admit -- has no expression here at all.
#
# Applying this file alone therefore produces spans with the right attribute
# names carrying values the target schema does not define. That is not a bug
# in the generator; it is the shortfall, written out.

# WHAT THIS FILE CANNOT SAY (12 mappings)
#
#   ambiguous_precedence
#     gen_ai.request.frequency_penalty         gen_ai.request.frequency_penalty wins over llm.frequency_penalty, and attribute_map cannot say so
#     gen_ai.request.presence_penalty          gen_ai.request.presence_penalty wins over llm.presence_penalty, and attribute_map cannot say so
#     gen_ai.request.stream                    gen_ai.is_streaming wins over llm.is_streaming, and attribute_map cannot say so
#
#   no_attribute
#     gen_ai.prompt.version                    v1.41.0 defines no attribute for this field
#
#   not_a_name
#     gen_ai.agent.name                        produced by reading the span, not by renaming an attribute
#     gen_ai.input.messages                    produced by reading the span, not by renaming an attribute
#     gen_ai.operation.name                    produced by reading the span, not by renaming an attribute
#     gen_ai.output.messages                   produced by reading the span, not by renaming an attribute
#     gen_ai.response.finish_reasons           produced by reading the span, not by renaming an attribute
#     gen_ai.tool.definitions                  produced by reading the span, not by renaming an attribute
#     gen_ai.tool.name                         produced by reading the span, not by renaming an attribute
#
#   value_transform
#     gen_ai.provider.name                     lowercases the value
#
file_format: 1.1.0

# Version 1.0.0 of this schema is the normalized form. Version 0.0.0 is the
# openllmetry vocabulary the spans arrive in. Calling a vendor's dialect a prior
# version of the conventions is a liberty -- it is a sibling, not an
# ancestor -- and it is taken here because the format offers no other way to
# relate two vocabularies. That liberty is itself part of the argument.
schema_url: https://github.com/Grace/genai-interlingua/schemas/openllmetry/v1.41.0/1.0.0

versions:
  0.0.0:

  1.0.0:
    spans:
      changes:
        - rename_attributes:
            attribute_map:
              gen_ai.usage.cache_creation_input_tokens: gen_ai.usage.cache_creation.input_tokens
              gen_ai.usage.cache_read_input_tokens: gen_ai.usage.cache_read.input_tokens
              gen_ai.usage.completion_tokens: gen_ai.usage.output_tokens
              gen_ai.usage.prompt_tokens: gen_ai.usage.input_tokens
              gen_ai.usage.reasoning_tokens: gen_ai.usage.reasoning.output_tokens
              llm.chat.stop_sequences: gen_ai.request.stop_sequences
              llm.top_k: gen_ai.request.top_k
              traceloop.prompt.key: gen_ai.prompt.name
              traceloop.workflow.name: gen_ai.workflow.name
